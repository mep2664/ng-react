import * as React from "react";
import styled from "styled-components";

const Aside = styled.aside`
`;

export const ModuleAside: React.FC = () => {
    return (
        <Aside></Aside>
    );
}